---

# A RoleBinding to allow AWX
# to deploy to our namespace.

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: awx-sa
  namespace: "{{ infra_namespace }}"
roleRef:
  kind: Role
  name: im-psp-unrestricted
  apiGroup: rbac.authorization.k8s.io
subjects:
- kind: ServiceAccount
  name: awx
  namespace: "{{ infra_namespace }}"
