---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pg-pvc
  namespace: {{ infra_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: '{{ pg_vol_storageclass }}'
  resources:
    requests:
      storage: '{{ pg_vol_size_g }}Gi'
