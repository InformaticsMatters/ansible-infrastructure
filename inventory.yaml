---

# Our inventory.

all:

  children:
    nfs:
      hosts:
        130.246.213.182:
    rke:
      hosts:
        # etcd/ctrl/worker
        18.156.198.182:
      vars:
        ansible_user: ubuntu
        ansible_ssh_private_key_file: ~/.ssh/abc-im
    infra_server:
      hosts:
        localhost:
