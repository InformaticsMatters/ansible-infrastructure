---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pg-bu-pvc
  namespace: {{ infra_namespace }}
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: {{ pg_bu_vol_storageclass }}
  resources:
    requests:
      storage: '{{ pg_bu_vol_size_g }}Gi'
