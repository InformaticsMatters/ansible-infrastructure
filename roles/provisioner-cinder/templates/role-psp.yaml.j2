---

# A PSP Role

kind: Role
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  name: csi-cinder-psp
  namespace: "{{ cinder_namespace }}"
rules:
- apiGroups: ['policy']
  resources: ['podsecuritypolicies']
  verbs: ['use']
  resourceNames: ['{{ cinder_psp }}']
